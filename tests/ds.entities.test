<?php
// $Id$

/**
 * @file
 * Entities tests
 */

/**
 * Test node entity.
 */
class dsNodeTests extends dsBaseTest {

  /**
   * Implements getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => t('Node display'),
      'description' => t('Tests for display of nodes on full page, switch, search and views.'),
      'group' => t('Display suite'),
    );
  }

  function testDSNodeEntity() {

  }
}

/**
 * Test user entity.
 */
class dsUserTests extends dsBaseTest {

  /**
   * Implements getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => t('User display'),
      'description' => t('Tests for display of users on full page, search and views.'),
      'group' => t('Display suite'),
    );
  }

  function testDSUserEntity() {

  }
}

/**
 * Test node entity.
 */
class dsCommentTests extends dsBaseTest {

  /**
   * Implements getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => t('Comment display'),
      'description' => t('Tests for display of comments.'),
      'group' => t('Display suite'),
    );
  }

  function testDSCommentEntity() {

  }
}

/**
 * Test taxonomy entity.
 */
class dsTaxonomyTests extends dsBaseTest {

  /**
   * Implements getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => t('Taxonomy display'),
      'description' => t('Tests for display of taxonomies on full page and views.'),
      'group' => t('Display suite'),
    );
  }

  function testDSTaxonomyEntity() {

  }
}