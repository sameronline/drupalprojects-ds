<?php

/**
 * @file
 * Display Suite Devel
 */

/**
 * Implements hook_menu().
 */
function ds_devel_menu() {
  $items = array();

  $items['node/%node/devel/markup'] = array(
    'title' => 'Markup',
    'route_name' => 'ds_devel.markup'
  );
  $items['node/%node/devel/markup/%key'] = array(
    'title' => 'Markup',
    'route_name' => 'ds_devel.markup_view_mode'
  );

  return $items;
}
